name: flutter_application_33
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  webview_flutter: ^3.0.1
  url_launcher: ^6.1.0  
  kakaomap_webview: ^0.6.2
  intl: ^0.19.0
  flutter_vlc_player: ^7.4.2  # VLC Player 패키지 (버전은 최신 버전에 맞게 설정)
  path_provider: ^2.1.4  # 파일 경로 관리 패키지
  video_player: ^2.9.2
  ffmpeg_kit_flutter: ^6.0.3

  table_calendar: ^3.1.2


  qr_flutter: ^4.0.0  # 최신 버전 확인 후 사용
  
  flutter_secure_storage: ^9.2.2 # 
  http: ^0.13.3
  provider: ^6.1.2
  cached_network_image: ^3.2.0
  flutter_dotenv: ^5.2.1
  image_picker: ^1.1.2
  dio: ^5.7.0
  permission_handler: ^11.3.1 # 저장 권한도 요청
  image_gallery_saver: ^2.0.3
 
  geolocator: ^13.0.1

  shared_preferences: ^2.3.2
    
dev_dependencies:
  flutter_test:
    sdk: flutter  
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
